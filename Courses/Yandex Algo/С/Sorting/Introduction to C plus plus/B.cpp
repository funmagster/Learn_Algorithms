#include <iostream>
#include <fstream>

using namespace std;

int main() {
    freopen("divide.in", "r", stdin);
    freopen("divide.out", "w", stdout);

    int a, b;
    cin >> a >> b;
    cout << a / b;

    return 0;
}
